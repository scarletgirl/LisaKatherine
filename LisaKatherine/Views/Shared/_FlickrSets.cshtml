@using Webdiyer.WebControls.Mvc;
@model PagedList<LisaKatherine.Interface.IPhotoSet>
           
@foreach (var item in Model)
{
    <div>@item.SetName</div>
    <div>@Html.Raw(item.Description)</div>
}
