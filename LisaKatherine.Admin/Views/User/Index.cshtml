@model IEnumerable<LisaKatherine.Interface.IUser>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<h1>
    Users</h1>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table cellspacing="0">
    <tr>
        <th>
            Username
        </th>
        <th>
            Password
        </th>
        <th>
            First Name
        </th>
        <th>
            Last Name
        </th>
        <th>
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Username
            </td>
            <td>
                @item.Password
            </td>
            <td>
                @item.FirstName
            </td>
            <td>
                @item.LastName
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.UserId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.UserId })
            </td>
        </tr>
    }
</table>
